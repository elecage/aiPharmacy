# 약 종류 구분 AI 모델 훈련 절차 문서 2

## 1. 문서 목적

이 문서는 앱인벤터 약 종류 구분 앱에 사용할 AI 이미지 분류 모델을 훈련하는 방법을 정리한 문서이다.  
첫 번째 문서인 `앱인벤터와 인공지능 확장을 활용한 약 종류 구분 앱 개발 계획서 1`에서 정한 방향에 따라, AI 모델 훈련은 PC에서 진행하고 앱인벤터 앱은 훈련된 모델을 사용하여 약 사진을 분류한다.

이 문서의 목표는 다음과 같다.

- 약 이미지 데이터를 어떻게 준비할지 정한다.
- Teachable Machine에서 모델을 어떻게 훈련할지 정리한다.
- 훈련 결과를 어떤 기준으로 평가할지 정한다.
- 앱인벤터 연결을 위해 어떤 결과물을 저장해야 하는지 정리한다.
- 향후 문서에 첨부할 훈련 화면 이미지와 결과표 형식을 준비한다.

## 2. 훈련 방식 개요

본 프로젝트에서는 스마트폰 앱에서 직접 AI를 훈련하지 않는다.  
AI 모델 훈련은 PC에서 진행하고, 앱인벤터 앱은 훈련된 모델을 이용해 사진을 분석한다.

```text
PC
→ 약 이미지 수집
→ 약 종류별 폴더 정리
→ Teachable Machine에서 모델 훈련
→ 모델 테스트
→ 모델 내보내기

앱인벤터 앱
→ 사진 촬영 또는 선택
→ 훈련된 AI 모델로 분석
→ 약 종류 후보와 신뢰도 표시
```

PC 훈련 방식을 선택하는 이유는 다음과 같다.

- 이미지 파일을 한꺼번에 정리하기 쉽다.
- 클래스별 이미지 개수를 확인하기 쉽다.
- 잘못 들어간 사진을 삭제하거나 이동하기 쉽다.
- Teachable Machine 같은 웹 기반 훈련 도구를 사용하기 편하다.
- 모델을 여러 번 재훈련하고 비교하기 좋다.

## 3. 훈련 도구

### 3.1 기본 훈련 도구

| 구분 | 도구 | 역할 |
|---|---|---|
| 이미지 분류 모델 훈련 | Teachable Machine | 약 사진을 종류별로 학습 |
| 파일 정리 | PC 파일 탐색기 또는 Finder | 약 이미지 폴더 관리 |
| 결과 기록 | Markdown 또는 스프레드시트 | 테스트 결과 기록 |
| 앱 연결 | MIT App Inventor | 훈련된 모델 사용 |

### 3.2 Teachable Machine 선택 이유

Teachable Machine은 코딩 없이 이미지 분류 모델을 만들 수 있는 도구이다. 앱인벤터 프로젝트에서 AI 개념을 설명하고 시제품을 만들기에 적합하다.

다만 Teachable Machine 모델은 실제 의료용 약 식별 시스템 수준의 정확도를 보장하지 않는다. 따라서 이 프로젝트에서는 교육용, 실험용, 참고용 모델로 사용한다.

## 4. 훈련 대상 약 선정

처음부터 많은 약을 구분하려고 하면 데이터 수집과 정확도 관리가 어려워진다.  
초기 모델은 3개에서 5개 종류의 약만 선택한다.

### 4.1 초기 클래스 예시

```text
타이레놀
이부프로펜
아스피린
비타민C
알 수 없음
```

### 4.2 클래스 선정 기준

훈련할 약을 고를 때는 다음 기준을 사용한다.

- 사진으로 구분 가능한 차이가 있는가?
- 색상, 모양, 각인이 서로 다른가?
- 충분한 이미지를 확보할 수 있는가?
- 이름과 라벨을 명확히 붙일 수 있는가?
- 학생 프로젝트 또는 학습용으로 설명하기 적절한가?

### 4.3 `알 수 없음` 클래스

`알 수 없음` 클래스는 반드시 포함한다.  
AI 모델은 학습한 클래스 중 하나로 답을 내려고 하는 경향이 있기 때문에, 학습하지 않은 약이나 흐릿한 사진도 특정 약으로 잘못 판단할 수 있다.

`알 수 없음` 클래스에는 다음 이미지를 넣는다.

- 학습 대상이 아닌 다른 약 사진
- 약이 너무 흐릿하게 나온 사진
- 약이 너무 작게 나온 사진
- 배경만 있는 사진
- 약이 여러 개 섞여 있는 사진
- 손이나 포장지가 크게 보이는 사진

## 5. 이미지 데이터 준비

### 5.1 권장 이미지 개수

| 구분 | 최소 개수 | 권장 개수 |
|---|---:|---:|
| 약 종류별 이미지 | 50장 | 100장 이상 |
| 알 수 없음 이미지 | 50장 | 100장 이상 |
| 테스트 전용 이미지 | 약 종류별 10장 | 약 종류별 20장 이상 |

초기 시제품에서는 약 종류별 30장 정도로도 실험할 수 있지만, 정확도를 비교하기에는 부족할 수 있다. 문서와 발표까지 고려하면 약 종류별 50장 이상을 목표로 한다.

### 5.2 촬영 기준

약 사진은 실제 사용자가 앱으로 찍을 상황을 고려하여 다양하게 촬영한다.

- 정면에서 촬영
- 뒷면에서 촬영
- 왼쪽이나 오른쪽으로 조금 기울여 촬영
- 밝은 조명에서 촬영
- 어두운 조명에서 촬영
- 흰색 배경에서 촬영
- 어두운 배경에서 촬영
- 약이 화면 중앙에 있는 사진
- 약이 약간 작게 나온 사진

### 5.3 피해야 할 사진

다음 사진은 훈련 데이터로 사용하지 않는 것이 좋다.

- 약 이름을 잘못 알고 찍은 사진
- 여러 종류의 약이 한 장에 섞여 있는 사진
- 약보다 손, 포장지, 배경이 더 크게 나온 사진
- 너무 흔들려 약 모양을 알 수 없는 사진
- 인터넷에서 출처가 불분명한 사진
- 개인정보가 보이는 사진

## 6. PC 폴더 구조

훈련 데이터를 정리할 때는 폴더 이름을 클래스 이름과 같게 만든다.

권장 폴더 구조:

```text
drug_ai_dataset/
  train/
    타이레놀/
    이부프로펜/
    아스피린/
    비타민C/
    알 수 없음/
  test/
    타이레놀/
    이부프로펜/
    아스피린/
    비타민C/
    알 수 없음/
```

`train` 폴더는 모델 훈련에 사용한다.  
`test` 폴더는 훈련이 끝난 뒤 모델 성능을 확인하는 데 사용한다.

중요한 점은 테스트 이미지를 훈련에 사용하지 않는 것이다. 훈련에 사용한 사진으로만 테스트하면 실제 정확도보다 좋아 보일 수 있다.

## 7. 파일 이름 규칙

사진 파일 이름은 나중에 확인하기 쉽게 정한다.

예시:

```text
tylenol_front_001.jpg
tylenol_back_002.jpg
ibuprofen_bright_001.jpg
unknown_blur_001.jpg
```

한국어 파일 이름을 사용해도 되지만, 일부 웹 도구나 압축 과정에서 오류가 날 수 있으므로 영어와 숫자를 사용하는 방식을 권장한다.

## 8. Teachable Machine 훈련 절차

### 8.1 프로젝트 생성

1. Teachable Machine 사이트에 접속한다.
2. 새 프로젝트를 만든다.
3. `이미지 프로젝트`를 선택한다.
4. 표준 이미지 모델 방식을 선택한다.

참고 사이트: [Teachable Machine](https://teachablemachine.withgoogle.com/)

### 8.2 클래스 생성

약 종류별로 클래스를 만든다.

예시:

```text
Class 1: 타이레놀
Class 2: 이부프로펜
Class 3: 아스피린
Class 4: 비타민C
Class 5: 알 수 없음
```

클래스 이름은 앱 화면에 표시될 이름과 같게 만든다.  
앱에서 한국어 결과를 보여 줄 계획이므로 클래스 이름도 한국어로 입력하는 것이 이해하기 쉽다.

### 8.3 이미지 업로드

각 클래스에 해당하는 이미지를 업로드한다.

확인할 점:

- 다른 약 사진이 잘못 들어가지 않았는가?
- 같은 사진이 너무 많이 중복되지 않았는가?
- 약이 화면에서 충분히 크게 보이는가?
- 각 클래스의 이미지 수가 너무 차이나지 않는가?

### 8.4 모델 훈련

이미지를 모두 넣은 뒤 `Train Model`을 실행한다.

훈련 중에는 브라우저 창을 닫지 않는다.  
훈련이 끝나면 미리보기 화면에서 테스트 이미지를 넣어 결과를 확인한다.

### 8.5 1차 테스트

먼저 훈련에 사용하지 않은 테스트 이미지를 넣어 본다.

확인할 내용:

- 실제 약과 같은 결과가 나오는가?
- 신뢰도가 충분히 높은가?
- 비슷한 약을 헷갈리지 않는가?
- `알 수 없음` 이미지를 특정 약으로 잘못 판단하지 않는가?

## 9. 훈련 결과 평가 기준

### 9.1 기본 판정 기준

| 신뢰도 | 판정 |
|---:|---|
| 80% 이상 | 비교적 높은 신뢰도 |
| 50% 이상 80% 미만 | 후보로만 표시 |
| 50% 미만 | 판별 불가 |

이 기준은 앱 화면 표시 기준과 동일하게 사용한다.

### 9.2 테스트 결과 기록표

훈련 후 테스트 결과를 다음 표에 기록한다.

| 테스트 번호 | 실제 약 | AI 예측 | 신뢰도 | 판정 | 비고 |
|---:|---|---|---:|---|---|
| 1 | 타이레놀 | 타이레놀 | 91% | 성공 | 정면 사진 |
| 2 | 이부프로펜 | 아스피린 | 62% | 실패 | 어두운 조명 |
| 3 | 비타민C | 비타민C | 77% | 후보 | 배경 복잡 |
| 4 | 알 수 없음 | 알 수 없음 | 84% | 성공 | 학습 외 약 |

### 9.3 클래스별 정확도 기록표

| 약 종류 | 테스트 수 | 성공 수 | 실패 수 | 정확도 |
|---|---:|---:|---:|---:|
| 타이레놀 | 20 | 18 | 2 | 90% |
| 이부프로펜 | 20 | 15 | 5 | 75% |
| 아스피린 | 20 | 17 | 3 | 85% |
| 비타민C | 20 | 19 | 1 | 95% |
| 알 수 없음 | 20 | 14 | 6 | 70% |

`알 수 없음`의 정확도가 낮으면 실제 앱에서 위험할 수 있다. 학습하지 않은 약을 특정 약으로 잘못 판단할 수 있기 때문이다.

## 10. 모델 개선 방법

테스트에서 실패가 많이 발생하면 다음 방법으로 모델을 개선한다.

### 10.1 데이터 추가

틀린 상황과 비슷한 사진을 추가한다.

예를 들어 어두운 사진에서 자주 틀리면, 해당 약의 어두운 조명 사진을 더 촬영해 넣는다.

### 10.2 클래스 균형 맞추기

어떤 약은 100장이고 어떤 약은 20장뿐이면 모델이 데이터가 많은 클래스를 더 잘 맞힐 수 있다.  
가능하면 클래스별 이미지 수를 비슷하게 맞춘다.

### 10.3 배경 다양화

흰 배경 사진만 사용하면 실제 책상, 손바닥, 약봉지 위에서 찍은 사진을 잘 인식하지 못할 수 있다.  
여러 배경의 사진을 포함한다.

### 10.4 `알 수 없음` 강화

학습 대상이 아닌 약을 특정 약으로 잘못 판단하면 `알 수 없음` 클래스 데이터를 늘린다.

### 10.5 약 종류 줄이기

비슷한 약을 계속 헷갈리면 초기 버전에서는 약 종류를 줄인다.  
약 종류를 줄이고 정확도를 확보한 뒤 점차 확장하는 것이 좋다.

## 11. 모델 내보내기 계획

훈련이 끝난 모델은 앱인벤터 연결 방식에 맞게 내보낸다.

### 11.1 Android 확장 방식

Android 우선 개발에서는 앱인벤터 AI 확장 또는 Teachable Machine 관련 확장을 사용한다.  
확장이 요구하는 모델 형식과 연결 주소를 확인한 뒤 모델을 내보낸다.

확인할 항목:

- 확장이 TensorFlow.js 모델 주소를 요구하는가?
- 모델 파일을 앱 프로젝트에 업로드해야 하는가?
- 인터넷 연결이 필요한가?
- Companion과 빌드된 앱에서 모두 동작하는가?

### 11.2 WebViewer 방식

iPhone 대응을 검토할 때는 Teachable Machine 모델을 웹 페이지에서 실행하고, 앱인벤터의 WebViewer로 보여 주는 방식을 검토한다.

확인할 항목:

- iPhone Companion에서 WebViewer가 정상 실행되는가?
- 웹 페이지에서 카메라 또는 파일 선택이 가능한가?
- 분석 결과를 앱인벤터 화면으로 전달할 수 있는가?

### 11.3 Web API 방식

더 안정적인 iPhone 대응이 필요하면 외부 서버에서 AI 분석을 실행하고 앱인벤터는 결과만 받는 방식을 검토한다.

확인할 항목:

- 사진 업로드 방식
- 서버에서 모델 실행 방식
- 결과 JSON 형식
- 개인정보 보호
- 인터넷 연결 필요 여부

## 12. 문서에 첨부할 훈련 이미지 목록

최종 개발 문서에는 다음 이미지를 첨부한다.

| 그림 번호 | 이미지 내용 | 설명 |
|---|---|---|
| 그림 1 | Teachable Machine 프로젝트 생성 화면 | 이미지 프로젝트 선택 |
| 그림 2 | 클래스 구성 화면 | 약 종류별 클래스와 알 수 없음 클래스 |
| 그림 3 | 이미지 업로드 화면 | 약 종류별 학습 이미지 등록 |
| 그림 4 | 모델 훈련 실행 화면 | Train Model 실행 |
| 그림 5 | 미리보기 테스트 화면 | 테스트 이미지 분석 결과 |
| 그림 6 | 모델 내보내기 화면 | 앱인벤터 연결을 위한 모델 출력 |
| 그림 7 | 테스트 결과표 | 훈련 후 정확도 기록 |

각 그림 아래에는 다음 형식으로 설명을 작성한다.

```text
[그림 3] 약 종류별 이미지 업로드 화면

설명:
타이레놀, 이부프로펜, 아스피린, 비타민C, 알 수 없음 클래스를 만들고
각 클래스에 해당하는 사진을 업로드하였다.
```

## 13. 훈련 기록 양식

훈련할 때마다 아래 기록을 남긴다.

```text
훈련 번호:
훈련 날짜:
사용 도구:
클래스 목록:
클래스별 이미지 수:
테스트 이미지 수:
전체 정확도:
가장 많이 틀린 클래스:
개선할 점:
다음 훈련 계획:
```

예시:

```text
훈련 번호: 1차
훈련 날짜: 2026-05-17
사용 도구: Teachable Machine
클래스 목록: 타이레놀, 이부프로펜, 아스피린, 비타민C, 알 수 없음
클래스별 이미지 수: 각 50장
테스트 이미지 수: 각 10장
전체 정확도: 82%
가장 많이 틀린 클래스: 알 수 없음
개선할 점: 알 수 없음 데이터 추가 필요
다음 훈련 계획: 학습 대상이 아닌 약 사진 50장 추가
```

## 14. 안전 및 윤리 주의사항

약 종류 구분 앱은 의료적 위험을 가질 수 있으므로 다음 사항을 지킨다.

- AI 결과를 최종 판단으로 사용하지 않는다.
- 앱에는 반드시 전문가 확인 문구를 넣는다.
- 약 복용 여부를 앱이 결정하지 않는다.
- 사용자의 개인정보가 포함된 사진을 훈련에 사용하지 않는다.
- 처방전, 이름, 생년월일, 병원명 등이 보이는 사진은 제외한다.
- 인터넷에서 가져온 이미지는 출처와 사용 가능 여부를 확인한다.

앱에 표시할 권장 문구:

```text
이 앱은 약 종류를 참고용으로 추정합니다.
AI 결과는 틀릴 수 있습니다.
복용 전 반드시 약사 또는 의사에게 확인하세요.
```

## 15. 다음 단계

이 문서에 따라 AI 모델 훈련 준비가 끝나면 다음 문서에서는 앱인벤터 화면과 블록 설계를 작성한다.

다음 문서 제목:

```text
앱인벤터 한국어 화면 및 블록 설계 문서 3
```

다음 문서에는 다음 내용을 포함한다.

- 앱 화면 구성
- 한국어 컴포넌트 이름
- 버튼별 동작
- 사진 촬영 블록
- 이미지 선택 블록
- AI 분석 블록
- 결과 표시 블록
- iPhone 대응 대안 블록
- 문서에 첨부할 블록 이미지 목록
